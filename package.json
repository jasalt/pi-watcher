{
  "name": "pi-watcher",
  "version": "0.1.0",
  "description": "Editor comment watch mode for pi-coding-agent.",
  "type": "module",
  "main": "./dist/index.js",
  "keywords": ["pi-package", "pi", "extension", "watch", "editor"],
  "pi": {
    "extensions": ["./src/index.ts"],
    "image": "https://raw.githubusercontent.com/vedang/pi-watcher/main/images/banner.png"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --noEmit --watch --preserveWatchOutput",
    "test": "vitest run --config vitest.config.unit.ts",
    "test:watch": "vitest --config vitest.config.unit.ts",
    "test:coverage": "vitest run --config vitest.config.unit.ts --coverage"
  },
  "dependencies": {
    "chokidar": "^4.0.0",
    "ignore": "^7.0.0",
    "minimatch": "^10.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^24.3.0",
    "@earendil-works/pi-coding-agent": "0.80.6",
    "@vitest/coverage-v8": "^3.2.4",
    "jscpd": "^4.0.8",
    "knip": "^5.86.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
